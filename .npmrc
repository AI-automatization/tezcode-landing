# Don't hard-fail when the running pnpm differs from package.json "packageManager".
# Railway/Corepack activates pnpm@10.30.1; this is a safety net so a version
# mismatch degrades to a warning instead of aborting the build.
package-manager-strict=false
